1. Copiare _chat.txt in input/

2. npm run start
   → genera days.json e players.json

3. npm run validate
   → controllare le giornate duplicate

4. npm run import
   → sincronizza Firestore

5. Completare manualmente in Firestore:
   - result.winner
   - result.wrapTime
   - result.exactWinner